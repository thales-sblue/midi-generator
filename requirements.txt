mido>=1.3,<2.0
mcp>=2.1,<3.0
pytest>=8.0,<9.0
# Audio analysis (src/midi_generator/audio); see docs/AUDIO_ANALYSIS.md and
# docs/DEPENDENCY_POLICY.md.
librosa>=0.11,<0.12
numpy>=2.0,<3.0
soundfile>=0.13,<0.15
# scipy 1.18.1 wheels are blocked by Windows Application Control on the
# development machine (DLL load refused); 1.16.x loads. Revisit when pinning.
scipy>=1.15,<1.17
